{% extends 'base.html.twig' %}

{% block title %} What's up guys {% endblock %}

{% block body %}
<h1>Starshop, There you go!</h1>

<div>
    Browse through {{ships|length * 10 }} starships!
</div>

{% if ships|length > 20 %}

{# Do something #}
    <div> That's awesome <div>
{% endif %}


<div>

<h2>My ship</h2>
    <table>
        <tr>
            <th>Name</th>
            <td>{{ myShip.name | upper }}</td>
        </tr>
        <tr>
            <th>Class</th>
            <td>{{ myShip.class }}</td>
        </tr>
        <tr>
            <th>Captain</th>
            <td>{{ myShip.captain }}</td>
        </tr>
        <tr>
            <th>Status</th>
            <td>{{ myShip.status }}</td>
        </tr>
    </table>
<div>
{% endblock %}